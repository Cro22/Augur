// Command augur is the CLI entrypoint for the Augur cost gate.
//
// It is a placeholder until the runner/proxy/projection/gate subcommands land
// (see SPEC.md, hitos 1–4). Hito 0 ships only the cost foundation in ./cost.
package main

import "fmt"

func main() {
	fmt.Println("augur: cost-first FinOps gate for AI agents (scaffold — see SPEC.md)")
}
